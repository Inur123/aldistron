import { Routes } from '@angular/router';
import { VendorListComponent } from './ui/vendor-list/vendor-list.component';

export const VENDOR_ROUTES: Routes = [
  {
    path: '',
    component: VendorListComponent
  }
];
