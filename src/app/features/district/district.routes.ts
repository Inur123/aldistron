import { Routes } from '@angular/router';
import { DistrictListComponent } from './ui/district-list/district-list.component';

export const DISTRICT_ROUTES: Routes = [
  {
    path: '',
    component: DistrictListComponent
  }
];
