import { Routes } from '@angular/router';
import { TokenListComponent } from './ui/token-list/token-list.component';

export const TOKEN_ROUTES: Routes = [
  {
    path: '',
    component: TokenListComponent
  }
];
